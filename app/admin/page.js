'use client';
import {useEffect,useState} from 'react';
import {createClient} from '@supabase/supabase-js';
const supabase=createClient(process.env.NEXT_PUBLIC_SUPABASE_URL,process.env.NEXT_PUBLIC_SUPABASE_ANON_KEY);
export default function Admin(){const [session,setSession]=useState(null),[email,setEmail]=useState(''),[password,setPassword]=useState(''),[file,setFile]=useState(null),[msg,setMsg]=useState('');useEffect(()=>{supabase.auth.getSession().then(({data})=>setSession(data.session));const {data}=supabase.auth.onAuthStateChange((_e,s)=>setSession(s));return()=>data.subscription.unsubscribe()},[]);async function login(e){e.preventDefault();setMsg('Login...');const {error}=await supabase.auth.signInWithPassword({email,password});setMsg(error?.message||'Login berhasil.')}async function upload(e){e.preventDefault();if(!file)return;setMsg('Mengupload dan menyimpan data...');const {data:{session}}=await supabase.auth.getSession();if(!session){setMsg('Sesi login sudah berakhir.');return}const fd=new FormData();fd.append('file',file);const res=await fetch('/api/admin/upload',{method:'POST',headers:{Authorization:`Bearer ${session.access_token}`},body:fd});const data=await res.json();setMsg(data.ok?`Berhasil: ${data.rowCount.toLocaleString('id-ID')} baris, periode ${data.period}.`:data.error||'Upload gagal.')}return <main style={{maxWidth:760,margin:'60px auto',padding:24}}><h1>GRIR Admin</h1><p>Upload Excel ke database pusat Supabase.</p>{!session?<form onSubmit={login} style={{display:'grid',gap:12,maxWidth:420}}><input type="email" placeholder="Email admin" value={email} onChange={e=>setEmail(e.target.value)} required/><input type="password" placeholder="Password" value={password} onChange={e=>setPassword(e.target.value)} required/><button type="submit">Login</button></form>:<form onSubmit={upload} style={{display:'grid',gap:12,maxWidth:520}}><input type="file" accept=".xlsx,.xls" onChange={e=>setFile(e.target.files?.[0]||null)} required/><button type="submit">Upload Excel</button><button type="button" onClick={()=>supabase.auth.signOut()}>Logout</button></form>}<p>{msg}</p><a href="/">← Kembali ke Dashboard</a></main>}
